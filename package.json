{
  "name": "brms",
  "version": "1.0.0",
  "private": true,
  "description": "Browser Runtime MCP Server — Chrome Extension + Native Messaging Host",
  "type": "module",
  "workspaces": [
    "shared",
    "host",
    "extension"
  ],
  "scripts": {
    "build": "npm run build --workspace=host",
    "build:host": "npm run build --workspace=host",
    "start": "npm run start --workspace=host",
    "dev": "npm run dev --workspace=host"
  },
  "license": "MIT"
}
